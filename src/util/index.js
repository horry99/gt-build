let sum = (a, b) => {
    return a + b + 'sum'
}
let minus = (a, b) => {
    return a - b + 'minus'
}
let cheap = (a, b) => {
    return a - b + 'cheap'
}

export default{
    sum,
    minus,
    cheap
}